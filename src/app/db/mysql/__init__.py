"""Paquete `src.app.db.mysql`.

Este `__init__.py` se deja intencionalmente minimalista para evitar
efectos secundarios al importar la capa de persistencia. Las definiciones
de modelos, repositorios y cualquier inicialización específica deben
colocarse en módulos concretos (por ejemplo `models.py`, `repositories.py`).
"""
