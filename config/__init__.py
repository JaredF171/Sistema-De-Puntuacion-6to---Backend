"""Paquete `config`.

Este archivo `__init__.py` se mantiene intencionalmente sencillo.
No debe contener lógica de inicialización que cause efectos secundarios
durante las importaciones (por ejemplo, en el análisis estático o las
migraciones). Si necesita ejecutar código al iniciar la aplicación,
impleméntelo en una clase `AppConfig` dentro de `apps.py`.
"""
